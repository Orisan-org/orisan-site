# CLAIMS.md

Every factual assertion that appears on orisan.org gets a row here before it ships.
No row, no claim on the page. This is the mechanism behind the honest-finish bar.

Statuses: `VERIFIED` (checked against a live source, with date) · `UNVERIFIED`
(do not put on the page) · `STALE` (was true, needs recheck before launch) ·
`REMOVED` (the sentence is gone from the page; the row stays, naming the date and
the commit that removed it).

A removed row is never deleted. The record that a claim once shipped, and when it
stopped, is the useful part — deleting it would leave no trace that the page ever
said it.

Agents may add rows with status `UNVERIFIED`. Only a human may set `VERIFIED`.

| # | Claim as written on the page | Where it appears | Source | Status | Checked |
|---|---|---|---|---|---|
| 1 | Package installs as `uvx orisan-mcpscan` | mcpscan section, install block | pypi.org/project/orisan-mcpscan | VERIFIED | 2026-08-04 |
| 2 | Published version is 0.1.1, alpha, published 9 August 2026 | mcpscan section, status section | Live fetch, pypi.org/pypi/orisan-mcpscan/json (2026-08-10): `info.version` = `0.1.1`; `releases` = `['0.1.0', '0.1.1']`; `releases['0.1.1'][0].upload_time_iso_8601` = `2026-08-09T22:24:59.363550Z`. Clean venv, `uv pip install --no-cache orisan-mcpscan` → `orisan-mcpscan==0.1.1`; `mcpscan version` → `0.1.1`. | VERIFIED (agent, live fetch + clean-venv install per CLAUDE.md v2) | 2026-08-10 |
| 3 | Scanner is deterministic, no LLM in scanner logic | mcpscan section | mcpscan invariants, cold verification | VERIFIED | see Notion |
| 4 | No findings are suppressed; verdicts adjust severity, never hide | mcpscan section | 1:1 adjudication map | VERIFIED | see Notion |
| 5 | `payload_stored=false` on every finding; no telemetry by default | privacy line | Observed in live output | VERIFIED | 2026-08-04 |
| 6 | Runs locally; uploads nothing | privacy line | README + observed behaviour | VERIFIED | 2026-08-04 |
| 7 | Coverage maps to OWASP MCP01/02/03/05/07/09/10 | checks table | `mcpscan list-checks` | VERIFIED | 2026-08-04 |
| 8 | Streamable HTTP is a tested remote transport | transports, status section | Was FALSE on 0.1.0 (unpinned `mcp>=1.0.0` resolved to 2.0.0, which renamed the client entry point). **Re-verified on 0.1.1, 2026-08-10.** Clean venv from PyPI (`orisan-mcpscan==0.1.1`, `mcp==1.29.0`), scanned a live Streamable HTTP MCP server: `--transport http` returned `transport: http`, `server: remote-risky-server`, 7 findings, grade `D`, exit 1. Published metadata now declares `mcp[cli]<2,>=1.0.0` (pypi.org/pypi/orisan-mcpscan/0.1.1/json). | VERIFIED (agent, live command + fetch per CLAUDE.md v2) | 2026-08-10 |
| 9 | Terminal output shown on the page | proof section | Must be a verbatim copy of a real run, with the command and date recorded here | UNVERIFIED | — |
| 10 | The reactor "safety control rod axe man" backronym | catastrophe spine marginalia | Disputed folklore. Use the imagery; do not assert the etymology as fact. | UNVERIFIED — do not assert | — |
| 11 | Test counts, supported Python versions | anywhere | CI matrix must actually test every version claimed | STALE | — |
| 12 | "mcpscan reads what a server exposes — tools, resources, prompts — and runs deterministic checks before an AI agent connects" | hero lede | Live fetch, pypi.org/pypi/orisan-mcpscan/json (2026-08-07): summary "an alpha, local-first security scanner for Model Context Protocol servers"; description "enumerates exposed tools/resources/prompts/metadata, runs deterministic checks, and emits terminal, JSON, or Markdown findings before an AI agent trusts that server". Cross-refs rows 3, 7. Deliberately omits transports (row 8). | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-07 |
| 13 | "On your machine, uploading nothing" | hero lede | Rows 5 and 6, human-verified 2026-08-04: payload_stored=false observed in live output; runs locally, uploads nothing. | VERIFIED (via rows 5–6) | 2026-08-07 |
| 14 | Hero primary link: github.com/Orisan-org is Orisan's public GitHub organisation | hero link | Anonymous fetch 2026-08-07: org page loads publicly; public repo orisan-site ("Orisan website and Scout landing pages"). NOTE: PyPI 0.1.0 metadata links github.com/Orisan-org/mcpscan, which 404s anonymously — do not link product source anywhere until that repo is public; fix the URL in 0.1.1. | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-07 |
| 15 | "Quidquid id est, timeo Danaos et dona ferentis." — Virgil, Aeneid II.49 | ~~catastrophe spine marginalia~~ — REMOVED FROM THE PAGE in slice 4b, replaced by row 20 (the golem). Row kept for the record; the verification below still stands if the line is ever used again. | Live fetch, thelatinlibrary.com/vergil/aen2.shtml (2026-08-07): exact line at Book 2 line 49, Laocoön's warning about the horse. | VERIFIED, NOT ON THE PAGE | 2026-08-07 |
| 16 | Therac-25, 1985–87: six massive overdoses, three fatal; software reused from earlier machines whose hardware interlocks masked its faults | catastrophe spine marginalia | Live fetch, en.wikipedia.org/wiki/Therac-25 (2026-08-07): "at least six incident overdoses of radiation between 1985 and 1987"; "in three cases, the injured patients later died as a result of the overdose"; "reused modules and code routines from the Therac-20"; "Previous models had hardware interlocks to prevent such faults, but the Therac-25 had removed them". Canonical source named there: Leveson & Turner, "An Investigation of the Therac-25 Accidents" (1993). | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-07 |
| 17 | SCRAM "safety control rod axe man" presented as folklore: historians doubt it, the man who held the axe felt foolish | catastrophe spine marginalia | Live fetch, en.wikipedia.org/wiki/Scram (2026-08-07): NRC historian favours the slang origin; axe-man story framed as urban legend; Hilberry: "I don't believe I have ever felt quite as foolish as I did then... I did not get the SCRAM story until many years after the fact." Copy hedges per row 10: imagery only, etymology never asserted. | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-07 |
| 18 | Meta description: "Local-first security instruments for AI agents. The first, mcpscan, reads what an MCP server exposes — tools, resources, prompts — before an agent connects." | site metadata | Same evidence as rows 12–13 (PyPI summary/description fetched 2026-08-07; local-first and pre-connection enumeration are the package's own published description). | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-07 |
| 19 | "No forms, no analytics, no third-party scripts on this site." | contact page | Verified against the built output (2026-08-07): grep of .next chunks and rendered HTML finds zero analytics SDK references (segment.io/gtag/plausible/posthog/hotjar: 0 matches; the word "segment" appears only as Next router vocabulary) and zero external script src attributes. No form elements exist in any page source. Re-verify at launch. | VERIFIED (agent, build inspection per CLAUDE.md v2) | 2026-08-07 |
| 20 | The golem obeys the letter of its instruction; animated by a word on paper; stopped by taking that word back out; "in the oldest written telling it grew until it killed the man who made it, in the act of being shut down" | catastrophe spine marginalia (first entry) | Live fetch, en.wikipedia.org/wiki/Golem (2026-08-07): "Golems are not intelligent; if commanded to perform a task, they will perform the instructions literally"; the name of God "written on a piece of paper and inserted into the mouth or forehead of the golem"; deactivation by "removing the aleph (א), thus changing the inscription from 'truth' to 'death'". Oldest attested account = Golem of Chełm (Rabbi Eliyahu, 1550–1583), reported by Christoph Arnold: it "kept on growing and getting stronger", the rabbi commanded it to remove its boots, "and when the golem returned and turned into clay, all its weight fell on the rabbi and crushed him" — hence "oldest written telling" in the copy. The Prague/Rabbi Loew version is described there as primarily a modern literary invention (first written accounts 1834 onward) and is deliberately NOT cited. Framed throughout as folklore, never as history. | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-07 |
| 21 | The nine checks table: every ID, title, severity, capability and OWASP class | "What it reads" checks table | `COLUMNS=200 uvx --from orisan-mcpscan mcpscan list-checks` against the live PyPI package, 2026-08-09, version confirmed `0.1.0` via `mcpscan --version`. Table transcribed cell-for-cell from that output. **Discrepancy recorded:** the published README's own table differs from the CLI in four places — MCP-010 capability README "per finding" vs CLI `other`; MCP-030 README "shell_exec/code_eval" vs CLI `shell_exec`; MCP-050 title README adds "using a curated seed list"; MCP-002 status README "active with `--baseline`" vs CLI `active`. The page follows the CLI, because that is what a reader reproduces by running the command. Worth reconciling in 0.1.1. | VERIFIED (agent, live command) | 2026-08-09 |
| 22 | "MCP-002 runs only when you give it a previous JSON report with --baseline (or scan-config --baseline-dir); with no baseline the check does not fire" | checks caveats | Published README, pypi.org/pypi/orisan-mcpscan/json (fetched 2026-08-09): "MCP-002 runs only when you provide a previous JSON report with `--baseline` or use `scan-config --baseline-dir`." | VERIFIED (agent, live fetch) | 2026-08-09 |
| 23 | "MCP-050 compares exposed names against a curated static seed list held in the package. It does not monitor package registries, and it is not ecosystem coverage" | checks caveats | Published README (2026-08-09): "MCP-050 is an offline heuristic that compares exposed server/tool names against a curated static seed list of common MCP server names. It does not monitor package registries and should not be treated as exhaustive ecosystem coverage." | VERIFIED (agent, live fetch) | 2026-08-09 |
| 24 | Coverage maps to MCP01, 02, 03, 05, 07, 09, 10; MCP04, MCP06, MCP08 out of scope for the alpha | checks caveats | Two independent live sources agreeing: the OWASP column of `mcpscan list-checks` contains exactly those seven classes, and the published README states "Current coverage maps to OWASP MCP classes MCP01, MCP02, MCP03, MCP05, MCP07, MCP09, and MCP10. MCP04 supply chain analysis, MCP06 intent/flow issues, and MCP08 audit/telemetry gaps are out of scope for this alpha." | VERIFIED (agent, live command + fetch) | 2026-08-09 |
| 25 | The four contextual verdicts and their definitions | "How it grades" | Published README (2026-08-09), quoted definitions for `expected_by_purpose` ("inherent to the declared purpose and the check is downgrade-eligible"), `unexpected` ("outside the purpose category, but the declared text mentions it"), `undeclared` ("outside the purpose category and not mentioned in declared text"), `unadjudicated` ("no declared purpose was available"). All four strings also present in the installed package source (`mcpscan/adjudicate.py`). | VERIFIED (agent, live fetch + package source) | 2026-08-09 |
| 26 | "Nothing is suppressed. Severity is adjusted, and both values are kept" | "How it grades" | Published README (2026-08-09): "`mcpscan` does not suppress findings. It labels each finding with a contextual verdict and keeps both original and adjusted severity when they differ." Finding schema in the same README lists both `original_severity` and `adjusted_severity`. | VERIFIED (agent, live fetch) | 2026-08-09 |
| 27 | Grade scale is A, B, C, D, F — there is no E — and the thresholds A none / B low or info / C medium / D high / F critical, computed on the adjusted severity | "How it grades" grade stamps | Published package source, `mcpscan/scoring.py` from `uv pip install --target` of orisan-mcpscan 0.1.0 (2026-08-09): `grade_for()` returns "F" on CRITICAL, "D" on HIGH, "C" on MEDIUM, "B" on LOW or INFO, else "A"; `effective_severity()` returns `adjusted_severity or severity`. `config_scanner.py` line 23: `GRADE_ORDER = {"A": 0, "B": 1, "C": 2, "D": 3, "F": 4}`. **No E is emitted anywhere.** Our GradeStamp component previously accepted E and the components page rendered it; corrected in this slice. | VERIFIED (agent, package source) | 2026-08-09 |
| 28 | "No LLM anywhere in the scanner logic. Every check and every verdict is deterministic" | "What it will not do" | Proof rather than quotation: `grep -rniE "openai\|anthropic\|litellm\|langchain\|transformers\|torch\|llm"` across `mcpscan/*.py` in the installed 0.1.0 returns **zero** matches outside comments (2026-08-09). Published README corroborates: the tool does not "use LLM verdicts". | **UNVERIFIED — evidence class (denylist grep); see the audit below** | 2026-08-12 |
| 29 | "Scans run locally and contact nothing but the MCP server you point them at. One opt-in exception: --push-envelope posts the report to a control-plane URL you specify" | "What it will not do" | Package source audit (2026-08-09): the only outbound HTTP call site in `mcpscan/*.py` is `httpx.post` inside `_push_envelope()` (`cli.py:344`), reachable only via the `--push-envelope` flag. README: "By default, `mcpscan` runs locally. It does not upload source code, prompts, secrets, raw MCP responses, or findings to Orisan or any external service." **The qualifier is deliberate**: a bare "nothing is uploaded" would be false given the opt-in flag. | VERIFIED (agent, package source + live fetch) | 2026-08-09 |
| 30 | "Every finding carries payload_stored=false" | "What it will not do" | Published README (2026-08-09): finding schema lists `payload_stored=false`, and "Every finding sets `payload_stored=false`." Cross-refs row 5 (human-verified in live output 2026-08-04). | VERIFIED (agent, live fetch; corroborates row 5) | 2026-08-09 |
| 31 | "No runtime enforcement, no registry monitoring, no dashboards; it does not block agent actions or modify the target server" | "What it will not do" | Published README (2026-08-09): "`mcpscan` does not secure the model, enforce runtime policy, block agent actions, modify the target server, monitor registries, upload findings, or use LLM verdicts." And: "Dynamic probing, HTML reports, registry monitoring, GitHub Action packaging, SaaS dashboards, and runtime enforcement are not part of this alpha release." | VERIFIED (agent, live fetch) | 2026-08-09 |
| 32 | Streamable HTTP is BROKEN on a clean 0.1.0 install today | NOT ON THE PAGE — evidence for the blocked slice 3 and the section 4 draft | Live reproduction 2026-08-09, strengthening row 8 from mechanism to observation. Published metadata pins `mcp[cli]>=1.0.0` (unpinned); a clean `uv pip install orisan-mcpscan` resolves **mcp 2.0.0**. `mcpscan/connectors/remote.py:73` imports `streamablehttp_client` from `mcp.client.streamable_http`; in mcp 2.0.0 that module defines `streamable_http_client` instead — `grep -c "def streamablehttp_client"` returns **0**. stdio is unaffected: `stdio_client` still exists in mcp 2.0.0, as does `sse_client`. | VERIFIED FALSE — DO NOT CLAIM until 0.1.1 | 2026-08-09 |

| 33 | "A well-behaved server can be graded F for doing its job": purpose derived from server metadata is printed but not used for adjudication | "Status" known defects | Reproduced live 2026-08-09 against a real third-party server, the official @modelcontextprotocol/server-filesystem, not a fixture. `mcpscan scan --command "<abs path>/mcp-server-filesystem /tmp/safe-root"` -> header `Purpose: filesystem (server_info)`, Grade **F**, `write_file` and `edit_file` each `CRITICAL (was HIGH) undeclared`. Same command plus `--purpose-category filesystem` -> header `Purpose: filesystem (flag)`, Grade **B**, the same tools `INFO (was HIGH) expected_by_purpose`. Mechanism in the shipped source: `adjudicate.py:38-40` gates the `expected_by_purpose` branch on `profile.category_source == PurposeSource.FLAG`, and `PurposeSource.SERVER_INFO` is a distinct enum member (`models.py:45-48`), so a metadata-derived purpose can never downgrade and falls through to `undeclared`, which escalates. | VERIFIED (agent, live reproduction + source) | 2026-08-09 |
| 34 | "Remote scanning over Streamable HTTP fails on a clean install; stdio is unaffected" | "Status" known defects | Same evidence as row 32, verified 2026-08-09: unpinned `mcp[cli]>=1.0.0` resolves to mcp 2.0.0; `connectors/remote.py:73` imports `streamablehttp_client`, which mcp 2.0.0 does not define (it exports `streamable_http_client`); `grep -c "def streamablehttp_client"` = 0. stdio unaffected because `stdio_client` still exists in mcp 2.0.0. | VERIFIED (agent, live reproduction) | 2026-08-09 |
| 35 | scan-config env defect | NOT ON THE PAGE — could not be evidenced | **Did not reproduce**, 2026-08-09, three configurations against the official filesystem server via `scan-config --yes`: npx with no env block -> `1 total, 1 scanned, 0 failed`; absolute binary with no env block -> scanned; npx with explicit PATH and HOME -> scanned. Two candidate mechanisms were tested and ruled out: the SDK **merges** rather than replaces (`stdio_client` does `env=get_default_environment() | (server.env or {})`, and `DEFAULT_INHERITED_ENV_VARS` already includes `PATH` and `HOME` on POSIX), and cold-start timeout is implausible at a measured 8s cold npx start against a 90s default timeout. Omitted from the page per the rule that an unevidenced claim is cut, not softened. | UNVERIFIED — DO NOT SHIP | 2026-08-09 |

## Rules
- Version numbers, install commands, test counts and platform lists rot. Re-verify
  every row against live sources on launch day, not on the day it was written.
- If a claim cannot be verified, cut it. A shorter true page beats a longer impressive one.
- Never soften a false claim into a vague one. Remove it.
| 29 | Status section: the 0.1.0 grade-`F` defect is fixed in 0.1.1 — a standard filesystem server scanned with no flags reads `Purpose: filesystem (invocation)` and grades `B`, identical to passing the purpose explicitly | status section | Live command, 2026-08-10, from a clean venv holding `orisan-mcpscan==0.1.1` installed from PyPI: `mcpscan scan --command "npx -y @modelcontextprotocol/server-filesystem /tmp/orisan-site-safe-root"` → `Server: secure-filesystem-server v0.2.0` / `Purpose: filesystem (invocation)` / `Grade: B`. The same command with `--purpose-category filesystem` gives `Purpose: filesystem (flag)` / `Grade: B`. | VERIFIED (agent, live command per CLAUDE.md v2) | 2026-08-10 |
| 30 | Status section: a purpose a server claims for itself still cannot lower a severity, only raise one | status section | Package source of the published 0.1.1 wheel, `mcpscan/adjudicate.py`: `OPERATOR_PURPOSE_SOURCES = {PurposeSource.FLAG, PurposeSource.INVOCATION}` gates every downgrade; `server_info` and `config` sources take the `EXPECTED_UNCONFIRMED` branch which passes `original` as the adjusted severity. Stated in the same file as "Any purpose source may ESCALATE a severity. Only an operator-supplied purpose may DOWNGRADE one." | VERIFIED (agent, package source of the published artifact) | 2026-08-10 |
| 31 | Status section: if the SDK pin is bypassed, mcpscan refuses to scan and names the version it found | status section | Live command, 2026-08-10: clean venv with `orisan-mcpscan==0.1.1`, then `uv pip install --no-deps mcp==2.0.0` to force the pin open. `mcpscan scan http://127.0.0.1:9/mcp --transport http` → `Incompatible mcp SDK: mcpscan requires the mcp SDK >=1.0.0,<2, but mcp 2.0.0 is installed. ... mcpscan refuses to scan rather than report a result it cannot stand behind.` | VERIFIED (agent, live command per CLAUDE.md v2) | 2026-08-10 |
| 32 | Status section: the MCP SDK is pinned below version 2; adapting to 2.x is open work | status section | Live fetch, pypi.org/pypi/orisan-mcpscan/0.1.1/json (2026-08-10): `requires_dist` contains `mcp[cli]<2,>=1.0.0`. Open work confirmed by the refusal behaviour in row 31 — the tool declines rather than supports 2.x. | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-10 |
| 33 | Status section: SSE is wired through the SDK where available and is not integration-tested | status section | Live fetch of the published 0.1.1 long description, pypi.org/pypi/orisan-mcpscan/0.1.1/json (2026-08-10), transports table row: `| SSE | Wired through the MCP SDK when available; not integration-tested |`. | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-10 |
| 34 | Install line `uvx orisan-mcpscan scan-config ./mcp.json --yes`; the distribution is `orisan-mcpscan` and the command it installs is `mcpscan` | "Run it" section, install block | Live, 2026-08-10, on a machine with nothing installed: `uvx --refresh --from orisan-mcpscan mcpscan version` → `0.1.1`. Clean venv from PyPI, `uv pip install --no-cache orisan-mcpscan` → `orisan-mcpscan==0.1.1`, and the venv's `bin/` contains both `mcpscan` and `orisan-mcpscan`. The `scan-config ./mcp.json --yes` form is the published README's own headline one-liner (pypi.org/pypi/orisan-mcpscan/0.1.1/json). | VERIFIED (agent, live command per CLAUDE.md v2) | 2026-08-10 |
| 35 | The terminal block in the "Run it" section | "Run it" section, `<pre>` | **Verbatim stdout, pasted unedited.** Command: `COLUMNS=76 mcpscan scan-config ./mcp.json --yes --no-color`, run 2026-08-10 from a clean venv holding `orisan-mcpscan==0.1.1` (`mcp==1.29.0`) installed from PyPI. Config: two servers from the official MCP collection — `npx -y @modelcontextprotocol/server-memory`, and `npx -y @modelcontextprotocol/server-filesystem /`. Output on the page is byte-identical to the capture at `scratchpad/siteverify/config-output.txt` except the config filename, shown as `mcp.json` to match the command printed above it. Nothing reflowed, nothing tidied. | VERIFIED (agent, live command per CLAUDE.md v2) | 2026-08-10 |
| 36 | Grades in that run: memory store `A` with no findings, filesystem server `D` with five `HIGH` findings marked `expected_unconfirmed` | "Run it" section, grade stamps | Same run as row 35. Output shows `notes-memory ... Grade: A ... No findings.` and `risky-filesystem ... Grade: D` with five `MCP-010` rows at `HIGH` / `expected_unconfirmed`, and `Worst grade: D`. | VERIFIED (agent, live command per CLAUDE.md v2) | 2026-08-10 |
| 37 | "mcpscan reads the tools a server exposes, not the scope it was handed. Both of those servers would produce the same five findings pointed at a single folder." | "Run it" section, caveat | Live comparison, 2026-08-10, same 0.1.1 build: the filesystem server scoped to `/tmp/orisan-demo-root` produces the same five `MCP-010` findings on the same five tools (`edit_file`, `get_file_info`, `read_file`, `read_multiple_files`, `write_file`) as the same server scoped to `/`. The scan target path appears nowhere in the findings. Corroborated by the package source: `checks/dangerous_capabilities.py` reads tool names, descriptions and input schemas only, and never the invocation arguments. | VERIFIED (agent, live command + package source) | 2026-08-10 |
| 38 | "Nothing is installed permanently and nothing leaves the machine" for the `uvx` line | "Run it" section, install caveat | `uvx` runs the package in a throwaway environment and is documented as such by Astral (docs.astral.sh/uv/guides/tools). "Nothing leaves the machine" rests on rows 5 and 6 (payload_stored=false, runs locally, uploads nothing), re-confirmed on 0.1.1 in this session: `grep` of the installed 0.1.1 package finds a single outbound call site, `httpx.post` in `_push_envelope`, reachable only via the opt-in `--push-envelope` flag which is not used in the command shown. | VERIFIED (agent, package source of the published artifact) | 2026-08-10 |
| 39 | Home h1: "Your agent trusts every server in that config." | home hero | **A claim about MCP client behaviour, not about mcpscan — it cannot be verified against the 0.1.1 wheel.** Two live sources, fetched 2026-08-10. (a) MCP specification 2025-06-18, Server/Tools, *User Interaction Model*: "Tools in MCP are designed to be **model-controlled**, meaning that the language model can discover and invoke tools automatically based on its contextual understanding and the user's prompts." The safeguards there are advisory, not required: "there **SHOULD** always be a human in the loop with the ability to deny tool invocations", and clients **SHOULD** "Prompt for user confirmation on sensitive operations". Under *Data Types → Tool*: "clients **MUST** consider tool annotations to be untrusted unless they come from trusted servers" — the protocol defines no mechanism by which a server becomes trusted, so configuration is the trust decision. (b) Claude Code MCP documentation (code.claude.com/docs/en/mcp): "Verify you trust each server before connecting it," which places the vetting burden on the operator. **Counter-evidence, recorded rather than omitted:** the same page states "For security reasons, Claude Code prompts for approval in interactive sessions before using project-scoped servers from `.mcp.json` files." So at least one named client does gate project-scoped servers. That prompt approves a *server*, not its capabilities: it is a yes/no on a name, shows no tool list, and servers may change their tools afterwards: MCP specification 2025-06-18, Server/Tools, *List Changed Notification* — "When the list of available tools changes, servers that declared the `listChanged` capability **SHOULD** send a notification", method `notifications/tools/list_changed`. The specification places no obligation on the client to re-obtain approval when it arrives. The sentence claims trust, not absence of a dialog, and trust granted once at server level without sight of what the server exposes is what it describes. | VERIFIED (agent, live fetch per CLAUDE.md v2) | 2026-08-10 |
| 40 | Home hero: "mcpscan reads what each server actually exposes — tools, resources, prompts — runs deterministic checks over it and grades what it finds" | home hero | Rows 12, 3, 7 and 27, all re-confirmed on 0.1.1 in this session. Live run 2026-08-10 from a clean venv holding `orisan-mcpscan==0.1.1` from PyPI enumerates tools/resources/prompts and emits a grade; `mcpscan list-checks` returns the nine deterministic checks; `grade_for()` in the published wheel returns A–F with no model call anywhere in the path (row 28). | VERIFIED (agent, live command + package source) | 2026-08-10 |
| 41 | Home hero: "It runs on your machine and uploads nothing. Ten seconds, nothing installed." | home hero | Rows 5, 6 and 38, re-confirmed on the published 0.1.1 wheel 2026-08-10: a single outbound call site exists in `src/`, `httpx.post` in `_push_envelope`, reachable only via the opt-in `--push-envelope` flag which the shown command does not use. "Nothing installed" is the documented behaviour of `uvx`, which runs a package in a throwaway environment. "Ten seconds" is the warm-cache case; the first run downloads the sample servers via `npx` and takes longer, which the published README states.  **REMOVED from the page 2026-08-13 by `658fdc9` ("Home: nav, hero and ticker"), which deleted the hero line carrying it. Row kept, not deleted. The reason the sentence went is recorded on 2026-08-16: "about ten seconds" absorbs a measured 6.33 s and a measured 9.14 s equally, so it cannot be wrong, and a claim that cannot be wrong is not a claim. Superseded in substance by the recorded run in `evidence/2026-08-16-mcpscan-real-run/`, whose 6.33 s is a record of one machine rather than a prediction about the reader's.** | **REMOVED 2026-08-13 (`658fdc9`)** — was VERIFIED (agent, package source + live run) | 2026-08-10 |
| 42 | Home: "It reports. It does not stop anything." and "A grade is knowledge, and knowledge is not a brake." | mcpscan section | Proof by absence, checked against the published 0.1.1 wheel 2026-08-10: the package exposes `scan`, `scan-config`, `list-checks` and `version` and no command that intercepts, blocks or proxies an action. `grep` of the installed package finds no proxy, hook, middleware or interception path. The tool connects to a server, enumerates it, and exits. | VERIFIED (agent, package source) | 2026-08-10 |
| 43 | Home: every "Planned · does not exist yet" ledger entry, the "The plan · not built yet" three-verb section, and the closing "The kill switch for AI" panel | home ledger, plan, closing panel | Nothing to verify against an artifact: these describe behaviour that does not exist, and the page says so in those words. Recorded here so the absence of a source is deliberate rather than an omission. The standing rule that applies is the opposite one — no present-tense claim may appear in these sections. | VERIFIED (agent, by construction) | 2026-08-10 |
| 44 | Founder quote: "It did not exist, so we started building it. What exists so far only reads." | founder quote | Attributed speech, rewritten by the speaker's own publisher rather than edited as a quotation — the no-editing rule protects against misquotation, which is not possible when speaker and publisher are the same. The factual half rests on row 42: what exists reads and does not stop. | VERIFIED (via row 42) | 2026-08-10 |
| 45 | Home: "A client that asks before connecting asks once… The protocol then lets that list change underneath the approval: a server declaring `listChanged` should send `notifications/tools/list_changed` when its tools change, and nothing requires the client to ask a second time." | mcpscan section, MCP-002 passage | MCP specification 2025-06-18, Server/Tools, *List Changed Notification* (live fetch 2026-08-10): "When the list of available tools changes, servers that declared the `listChanged` capability **SHOULD** send a notification: `notifications/tools/list_changed`." The spec's *Capabilities* section defines `listChanged` as the server-declared flag for exactly this. Nothing in the tools specification requires a client to re-prompt on receipt; the client-side approval language in Claude Code's docs (row 39) describes a prompt "before using project-scoped servers", not one per tool-list revision. The claim is that no obligation exists, and the absence is the citation. | VERIFIED (agent, live fetch) | 2026-08-11 |
| 46 | Home: MCP-002 is "Tool definition drift", raised at HIGH, requires `--baseline`, and its remediation is to review the changed surface before trusting the server again | mcpscan section, MCP-002 passage | Published 0.1.1 wheel source, read 2026-08-11. `mcpscan/checks/registry.py`: `CheckCatalogueEntry(id="MCP-002", title="Tool definition drift", severity=Severity.HIGH, status="active", capability=Capability.SURFACE_DRIFT, owasp_mcp="MCP03")`. `mcpscan/surface.py` `_drift_finding()` emits the same id/title/severity with `remediation="Review the changed MCP tool surface before trusting this server again."` and `payload_stored=False`. Comparison is hash-based: `_hash_text` normalises whitespace then SHA-256; `_hash_json` canonicalises with sorted keys then SHA-256. The `--baseline` requirement is row 22. | VERIFIED (agent, package source of the published artifact) | 2026-08-11 |
| 47 | Home: MCP-002 "is the only check that cannot fire on a first run" | mcpscan section, MCP-002 passage | Published 0.1.1 wheel, 2026-08-11: MCP-002 is the sole member of `DEFERRED_CHECKS` in `checks/registry.py`; every other check is returned by `active_checks()` and runs against a single enumeration. Drift findings are produced only by `compare_tool_surface(surface, load_baseline_surface(baseline_path))`, which `scanner.scan_context` calls only when a baseline path is supplied. | VERIFIED (agent, package source) | 2026-08-11 |
| 48 | Vision: "Every part we add can raise suspicion. None of them can grant permission." and the three floor-supremacy cards | vision hero, floor supremacy | **First-party design commitment.** Not a claim about the world, so there is no external source and nothing to cite: this is a public statement of how Orisan is designed to behave, and we are the source. Verification is a **test obligation against the built system** — the property must hold of what is actually built, and demonstrating that is adversarial testing, not a citation. Deliberately carries no source field and no internal pointer of any kind.  **PAGE DELETED `e7fa5fa`, 2026-08-17.** `/gap` and `/vision` were folded into Home — the ideas each page carried survive as rows 80, 81 and 82 — and both routes were removed. Row kept, not deleted: the record that this claim once shipped, and when it stopped, is the useful part. Evidence above is left exactly as it was verified. | **REMOVED 2026-08-17 (`e7fa5fa`)** — was COMMITMENT — verification is a test obligation | 2026-08-11 |
| 49 | Vision: "Parts of our composition-safety argument are not closed by design reasoning alone… Nothing here claims composition safety has been demonstrated." | vision, what-is-not-proven panel | **First-party design commitment**, and specifically a disclosure of residual. Publishes the shape of the residual and no address: no finding identifiers, no severities, no dispositions, no named areas. Verification is the same test obligation as row 48 — the honest position is that this is untested until adversarially tested, which is exactly what the sentence says.  **PAGE DELETED `e7fa5fa`, 2026-08-17.** `/gap` and `/vision` were folded into Home — the ideas each page carried survive as rows 80, 81 and 82 — and both routes were removed. Row kept, not deleted: the record that this claim once shipped, and when it stopped, is the useful part. Evidence above is left exactly as it was verified. | **REMOVED 2026-08-17 (`e7fa5fa`)** — was COMMITMENT — verification is a test obligation | 2026-08-11 |
| 50 | Vision: "the map inherits identity's assurance and can never exceed it, so the map cannot be trusted for enforcement until the actor-identity plane is real" | vision, the one hard dependency | **First-party design commitment.** A statement of how the design constrains itself: a structure keyed on actor identity can be no more trustworthy than that identity. Verification is a test obligation against the built system. **Disposition removed** — the previous version of this row paraphrased a risk disposition, and a paraphrased disposition is still a published disposition. The page body sentence carrying the same disposition was cut with it.  **PAGE DELETED `e7fa5fa`, 2026-08-17.** `/gap` and `/vision` were folded into Home — the ideas each page carried survive as rows 80, 81 and 82 — and both routes were removed. Row kept, not deleted: the record that this claim once shipped, and when it stopped, is the useful part. Evidence above is left exactly as it was verified. | **REMOVED 2026-08-17 (`e7fa5fa`)** — was COMMITMENT — verification is a test obligation | 2026-08-11 |
| 51 | Vision: the four intent items are unordered, and no dependency other than row 50 is asserted | vision, intent section | **First-party design commitment.** The page asserts exactly one ordering constraint, states it as architecture, and marks everything else as intent. No dates, no sequence numbers, no now/next/later appear anywhere on the page.  **PAGE DELETED `e7fa5fa`, 2026-08-17.** `/gap` and `/vision` were folded into Home — the ideas each page carried survive as rows 80, 81 and 82 — and both routes were removed. Row kept, not deleted: the record that this claim once shipped, and when it stopped, is the useful part. Evidence above is left exactly as it was verified. | **REMOVED 2026-08-17 (`e7fa5fa`)** — was COMMITMENT — verification is a test obligation | 2026-08-11 |
| 52 | **Governance record for rows 48-51.** | — | **Status contradiction — RESOLVED by founder ruling 2026-08-11.** The internal design corpus carries pages whose Notion `Status` property reads `Accepted` while their body reads "Reviewing. Non-final until founder acceptance", with unanswered founder-review blocks. Founder ruling: **the body is authoritative**; those pages are not accepted. Recorded as a ruling, not as a reconciliation of the two fields, which has not happened. **Sensitivity — CLOSED.** Rows 48-51 are first-party commitments carrying no source and no internal pointer, so no classified document is referenced and the question does not arise. **OPEN, founder action:** (a) the reverse pointer binding these public commitments back to the internal source belongs on the source page as a property listing derived public commitments; it is founder work, is deliberately not added here, and is recorded so the binding is not lost. (b) The internal review cadence is overdue — a `Next Review` of 2026-07-23 is 19 days past as of today, under `Change Control: Founder Approval`. Neither blocks Vision, because Vision no longer depends on those pages. | OPEN — founder action (a) and (b) | 2026-08-11 |
| 53 | Product: mcpscan "connects over stdio or Streamable HTTP, performs the MCP handshake and lists the tools, resources and prompts a server actually exposes" | product, shipped panel | Published 0.1.1 wheel, 2026-08-12. `mcpscan/models.py` `Transport` enum declares `stdio`, `sse`, `http`; `connectors/stdio.py` and `connectors/remote.py` perform `session.initialize()` then `list_tools`/`list_resources`/`list_prompts`. Live Streamable HTTP scan from a clean PyPI venv returned `transport: http`, `server: remote-risky-server`, 7 findings (row 8). SSE is deliberately not named on the page: the published README states it is not integration-tested (row 33). | VERIFIED (agent, package source + live command) | 2026-08-12 |
| 54 | Product: "nine deterministic checks" and "no model call decides whether something is a finding" | product, shipped panel | Live command against the published 0.1.1 wheel, 2026-08-12: `mcpscan list-checks` returns exactly nine distinct IDs — MCP-001, 002, 010, 020, 021, 030, 040, 041, 050 — with `mcpscan version` confirming `0.1.1`. Determinism and the absence of any model call rest on rows 3 and 28, re-confirmed on this artifact: `grep -rniE "openai\|anthropic\|llm"` over the installed package returns nothing in the scan path. | **PARTIAL — the nine-check count is a live command and stands; the no-LLM half is UNVERIFIED, evidence class** | 2026-08-12 |
| 55 | Product: "grades A to F on the adjusted severity" and "Terminal, JSON, Markdown or SARIF" | product, shipped panel | Published 0.1.1 wheel, 2026-08-12. `mcpscan/scoring.py` `grade_for()` returns F, D, C, B, A and nothing else, computed on `effective_severity()` which is `adjusted_severity or severity` (row 27). `cli.py` declares `--output` as `table, json, md, sarif` for `scan`. SARIF was confirmed as 2.1.0 in the release audit. | VERIFIED (agent, package source) | 2026-08-12 |
| 56 | Product: "Every finding carries redacted evidence only and sets payload_stored=false" and "It reports; it does not intervene in anything" | product, shipped panel | Rows 5, 8 and 42, re-confirmed on this artifact 2026-08-12: every finding in a live remote scan carries `payload_stored: false`. The no-intervention half is proof by absence — the published wheel exposes `scan`, `scan-config`, `list-checks` and `version`, and contains no proxy, hook, middleware or interception path. | VERIFIED (agent, live run + package source) | 2026-08-12 |
| 57 | Product: the loop section — "Watch" marked *One surface shipped*, "Decide" and "Stop" marked *Not built* | product, loop section | First-party design commitment for the two unbuilt verbs; nothing to verify against an artifact, and the page says so in those words. The "one surface shipped" qualifier is row 53: one transport family, one protocol, read before an agent connects rather than as it acts. | COMMITMENT — verification is a test obligation | 2026-08-12 |
| 58 | Product: "Anything we add can raise suspicion. Nothing we add can grant permission." | product, design-rule section | Same first-party design commitment as row 48. No source, no internal pointer; verification is a test obligation against the built system. | COMMITMENT — verification is a test obligation | 2026-08-12 |
| 59 | The gap: "Computed here by the same rules the installed package uses, not written by hand" | gap page, lede and widget | The widget is a port of the published 0.1.1 wheel's `adjudicate.py`, `purpose.py`, `scoring.py`, `utils/severity.py` and `data/purpose_categories.yaml`. Verified by execution rather than by inspection: six states were run against the wheel installed from PyPI and `tests/gap.spec.ts` asserts the page reproduces each. Ground truth, 2026-08-12 — `--purpose-category filesystem` → `filesystem (flag)`, grade B, info/expected_by_purpose; default invocation → `filesystem (invocation)`, grade B, identical rows; `scan-config` → `filesystem (config)`, grade D, high/expected_unconfirmed; opaque command with a filesystem server name → `filesystem (server_info)`, grade D, high/expected_unconfirmed; `--purpose "weather server"` → `unknown (flag)`, grade F, critical/undeclared on writes and high/unexpected on reads; no signal → `unknown (unknown)`, grade D, high/unadjudicated. If the port drifts from the product the test fails.  **PAGE DELETED `e7fa5fa`, 2026-08-17.** `/gap` and `/vision` were folded into Home — the ideas each page carried survive as rows 80, 81 and 82 — and both routes were removed. Row kept, not deleted: the record that this claim once shipped, and when it stopped, is the useful part. Evidence above is left exactly as it was verified. | **REMOVED 2026-08-17 (`e7fa5fa`)** — was VERIFIED (agent, live command + pinned test) | 2026-08-12 |
| 60 | The gap: the five findings, their IDs, targets and evidence strings | gap widget rows | Transcribed cell for cell from a live scan of `@modelcontextprotocol/server-filesystem` with the published 0.1.1 wheel (rows 35-37). Five `MCP-010` findings on `edit_file`, `get_file_info`, `read_file`, `read_multiple_files`, `write_file`, each at base severity `high`.  **PAGE DELETED `e7fa5fa`, 2026-08-17.** `/gap` and `/vision` were folded into Home — the ideas each page carried survive as rows 80, 81 and 82 — and both routes were removed. Row kept, not deleted: the record that this claim once shipped, and when it stopped, is the useful part. Evidence above is left exactly as it was verified. | **REMOVED 2026-08-17 (`e7fa5fa`)** — was VERIFIED (agent, live command) | 2026-08-12 |
| 61 | The gap: "Any source may raise a severity. Only you may lower one." and the config-is-not-you paragraph | gap page, rule panel | Behaviour of the published artifact rather than a design statement, and therefore verifiable: `adjudicate.py` in the installed 0.1.1 wheel declares `OPERATOR_PURPOSE_SOURCES = {PurposeSource.FLAG, PurposeSource.INVOCATION}` and gates every downgrade on it, while the escalation path is reached from any source. Rows 3 and 4 of the ground-truth table above are the observed consequence: `config` and `server_info` produce identical severities.  **PAGE DELETED `e7fa5fa`, 2026-08-17.** `/gap` and `/vision` were folded into Home — the ideas each page carried survive as rows 80, 81 and 82 — and both routes were removed. Row kept, not deleted: the record that this claim once shipped, and when it stopped, is the useful part. Evidence above is left exactly as it was verified. | **REMOVED 2026-08-17 (`e7fa5fa`)** — was VERIFIED (agent, package source + live command) | 2026-08-12 |
| 62 | Contact: "No forms, no analytics, no third-party scripts." and "the fonts are served from this domain" | contact page | Re-verified against the current build 2026-08-12, across all five pages. Rendered HTML for `/`, `/product`, `/gap`, `/vision`, `/contact`: **zero RESOURCE-LOADING attributes reach an external host** — no `src` on any element, no `<link rel=stylesheet|preload|preconnect>`, no external `url()` or `@font-face src` in the built CSS — and **zero** `<form>` elements. Anchor `href`s are navigation, not resource loading; three point at github.com and are covered by row 14. Fonts are self-hosted by `next/font`, so the design files' Google Fonts `<link>` and `preconnect` tags do not ship. **Recorded false positive:** a grep for analytics SDKs matched `plausible` in two `.next/server/**/*.js.map` source maps. That is the English word inside Next.js internals, not the analytics product — nothing matches in `.next/static`, which is what a browser loads. The pattern was too loose; the finding is not. Supersedes row 19's 2026-08-07 check.  **Addendum 2026-08-16, mechanism changed, origin did not.** The mono family moved from `next/font/google` to `next/font/local`, self-hosting a subset of JetBrains Mono 2.304 (see `app/fonts/NOTICE.md`). The claim on the page is about ORIGIN — "the fonts are served from this domain" — and it still holds: re-checked against the new build, all three font files resolve to same-origin `/_next/static/media/...`, including `JetBrainsMono_subset-s.p...woff2`. The sentence above reading "Fonts are self-hosted by `next/font`" also still holds, because `next/font/local` is `next/font`; it is recorded here anyway because it describes the mechanism and the mechanism changed. **Wording corrected 2026-08-16; the imprecision was PRE-EXISTING, introduced with this row on 2026-08-12 and not by the font change.** As written the row said "zero `src`/`href` attributes pointing at any external host", which its own build check contradicts: three anchor `href`s point at github.com. A row whose text fails its own check is the exact defect this file exists to prevent, so it was corrected rather than deferred. The claim on the page never said this and never changed; only this row's evidence sentence was wrong. **Re-run against the corrected wording on the new build, 2026-08-16:** 8 rendered HTML files and 1 built CSS file carrying 13 `@font-face` rules scanned — **0** resource-loading attributes reach an external host, 3 anchor `href`s do. The checker carries a positive control (a synthetic external stylesheet and script) which it catches, so a zero here is a measurement and not an instrument that cannot fail. | VERIFIED (agent, build inspection + rendered HTML) | 2026-08-12 |
| 63 | Contact: "mcpscan is public and installable now" | contact page, before-you-write panel | Rows 2 and 34, re-confirmed on the published artifact: `orisan-mcpscan` 0.1.1 resolves and installs from PyPI into a clean venv, and `github.com/Orisan-org/mcpscan` returns 200 anonymously. | VERIFIED (via rows 2, 34) | 2026-08-12 |
| 64 | Contact: "We keep a written pressure test on exactly that" | contact page, sceptic prompt | Internal document exists. Stated as a fact about us rather than a citation, and no title, link or identifier is published, per the publish-properties-never-findings rule. If the document is ever retired this sentence must go with it. | VERIFIED (agent, existence confirmed) | 2026-08-12 |
| 65 | "Nothing leaves your machine unless you send it." | home hero install block; home "know what it can reach" panel; /product privacy line | **Replaces the false claim "uploads nothing"**, which was wrong: `_push_envelope()` in the published 0.1.1 wheel is reachable from the CLI behind the opt-in `--push-envelope` flag, so the tool *can* transmit. The corrected wording states the condition rather than denying the capability. Verified against the installed 0.1.1 package: a single outbound call site, `httpx.post` in `_push_envelope`, reachable only via that flag, which is absent from the command shown on the page. | VERIFIED (agent, package source of the published artifact) | 2026-08-12 |
| 66 | "Ten seconds, nothing installed." for `uvx orisan-mcpscan scan-config ./mcp.json --yes` | home hero install block | Measured against the published 0.1.1 wheel from PyPI, on 2026-08-12, with an **empty uv cache** (`UV_CACHE_DIR` pointed at a fresh directory), which is what a first-time reader gets: **9.14 s wall clock**, `/usr/bin/time -p`, one stdio server in the config. Warm cache on the second run: **0.88 s**. "Ten seconds" is therefore an accurate ceiling for the cold path on this machine and network, not a floor. **Caveat recorded rather than hidden:** the dominant term is the package download, so a slower connection makes this longer; the number is a measurement, not a guarantee. The pitch deck's "sixty seconds" for the same command is not supported by this run and is the number that needs correcting.  **REMOVED from the page 2026-08-13 by `658fdc9` ("Home: nav, hero and ticker"), which deleted the hero line carrying it. Row kept, not deleted. The reason the sentence went is recorded on 2026-08-16: "about ten seconds" absorbs a measured 6.33 s and a measured 9.14 s equally, so it cannot be wrong, and a claim that cannot be wrong is not a claim. Superseded in substance by the recorded run in `evidence/2026-08-16-mcpscan-real-run/`, whose 6.33 s is a record of one machine rather than a prediction about the reader's.** | **REMOVED 2026-08-13 (`658fdc9`)** — was VERIFIED (agent, timed run against the published artifact) | 2026-08-12 |
| 67 | The seven surface lines in the home page's "Seven pieces" grid — Scout, Guard, Relay, Review, Control plane, The map, and mcpscan's own line | home, section 4, one line per cell | **These are design descriptions of intended function, not statements of availability.** One row rather than seven, because one claim is being made. The section's opening sentence states outright that six of the seven are design and that the line under each says what it is for rather than what it does yet. Verified against the repositories on 2026-08-13: seven pieces exist as an architecture; six have private repositories (`orisan-scout`, `orisan-guard`, `orisan-relay`, `orisan-review`, `orisan-control-plane` all return 404 unauthenticated) and one, The map, has no repository at all. **The `Shipping now` badge on mcpscan is the only availability claim on the page** and rests on row 65: public, installable, versioned 0.1.1 on PyPI. No other cell carries a badge, a status dot or a repository name, because each of those implies a repository a reader can find and six of them 404. **Staleness:** this row goes stale if a badge, dot or repo name is added to any other cell, or if a line is rewritten from purpose into behaviour. It does NOT go stale when a repository is published — publishing changes nothing here, because nothing here asserts publication. | VERIFIED (agent, GitHub API, 2026-08-13) | 2026-08-13 |
| 68 | "Deterministic. No model in the scanner. Nothing leaves your machine unless you send it." | home, section 4, the mcpscan cell | A factual claim about a shipping product, kept separate from row 67, which covers design descriptions of the other six. Each clause verified against `mcpscan-work` at `9649575` on 2026-08-13, and each stands alone. **Deterministic:** `tests/test_adjudicate.py:108 test_same_scan_emits_identical_json` scans the same context twice and asserts the rendered JSON is equal. Run: 7 passed. *Precisely:* the test normalises `scan.timestamp_utc` before comparing, so the findings, severities and grade are deterministic while the report embeds a wall-clock time — "deterministic" is claimed of the verdict, not of the file's bytes. **No model in the scanner:** zero imports of any inference library anywhere in `src/` (`openai|anthropic|litellm|langchain|transformers|torch|ollama|cohere`, none). `src/mcpscan/adjudicate.py` is a static severity map over keyword capabilities with an explicit trust invariant; no inference step exists in the decision path. **Nothing leaves your machine unless you send it:** the only outbound call in the package is `httpx.post` at `src/mcpscan/cli.py:364`, reachable only via `--push-envelope`, which is `typer.Option(... ) = False`. Default is no network. **FORBIDDEN PHRASING: `byte-identical`, said of a scan.** The precision above is exactly what that phrasing rules out — the report embeds a wall-clock time, so two runs of the same scan are not byte-identical and never will be. It is the phrasing a future writer reaches for when they want to sound rigorous, so it is named here to be hit before it ships. The bare word "Deterministic." is what the page carries: a security reader takes it to mean same server, same findings, same grade, which is what the test proves. *Not* forbidden of a transcript: row 35 says the page's terminal block is byte-identical to a captured file, which is a claim about transcription fidelity rather than about reproducibility, and it is sound. The two senses are different and only the first is ruled out. **"Offline" is deliberately NOT used** — it is ambiguous in exactly the direction that forced the "uploads nothing" correction, and scanning a remote server involves talking to it. | VERIFIED (agent, source + test run against mcpscan-work 9649575) | 2026-08-13 |
| 69 | Build-log date convention | home, section 8, all four entries | **Dates are UTC, and are the AUTHOR date of the substantive commit rather than the merge date.** UTC because a reader who checks will look at PyPI or the GitHub API and both report UTC; a page stating IST disagrees with every reader's own check. Author date because this repo rebases stacked branches constantly and merge dates move under a rebase — a build log whose dates change is not a record. Recorded as its own row so the next person does not re-derive it. No commit hashes appear on the page; they live in rows 70-73. | VERIFIED (convention, founder ruling 2026-08-13) | 2026-08-13 |
| 70 | "A gate that fails the build if any colour in the compiled CSS is not a declared token or a named exception." | home, section 8, 12 Aug 2026 | Commit `8baef35`, author date 2026-08-12 07:11:09 +0530 = **2026-08-12 01:41 UTC**, "Gate: every colour in built CSS must be a token or a named exception". Not `ff10261` two minutes earlier, which was a WIP commit that added the file: a build-log entry pointing at working code is a stronger artifact than one pointing at a WIP. The gate is `scripts/colour-allowlist.mjs`, still in the tree and still failing the build. | VERIFIED (agent, git log) | 2026-08-13 |
| 71 | "A gate that asserts the type scale is strictly monotonic at every width. Tuning one size alone had inverted it below 612px, where the larger step rendered smaller than the one beneath it." | home, section 8, 11 Aug 2026 | Commit `9c2eac47`, author date 2026-08-11 11:19:57 +0530 = **2026-08-11 05:49 UTC**, "Gate: assert the type scale is strictly monotonic at every width". The 612px crossing is evidenced in both that commit message and PR #28's body, which record the measured inversion — 320px: 4xl 41.60px against 3xl 44.80px; 390px: 44.18 against 47.95; 500px: 51.00 against 52.90 — and name `bd19f62` as the commit that steepened `4xl` alone. The entry states a self-inflicted defect and the gate that closed it, and both halves are in the record. | VERIFIED (agent, git log + PR #28 body) | 2026-08-13 |
| 72 | "mcpscan 0.1.1 on PyPI." | home, section 8, 9 Aug 2026 | `https://pypi.org/pypi/orisan-mcpscan/json`, `releases["0.1.1"]`, `upload_time_iso_8601` = **2026-08-09T22:24:59.363550Z** (wheel), 22:25:01 (sdist). Read from the registry, not from a changelog. **The reference page said 24 Jul 2026, wrong by sixteen days.** Three of its four dates were right, which is why every date is sourced rather than only the suspicious ones. | VERIFIED (agent, PyPI JSON API) | 2026-08-13 |
| 73 | "mcpscan 0.1.0 on PyPI. First public release." | home, section 8, 10 Jul 2026 | `https://pypi.org/pypi/orisan-mcpscan/json`, `releases["0.1.0"]`, `upload_time_iso_8601` = **2026-07-10T03:49:53.953110Z** (wheel), 03:49:55 (sdist). No earlier version exists in the registry, which is what supports "first public release". | VERIFIED (agent, PyPI JSON API) | 2026-08-13 |
| 74 | `team@orisan.org` as a working contact address | home, section 9, the contact CTA `mailto:` | Founder confirmed the mailbox live in the Zoho admin console, **2026-08-12**. **Supersedes the 2026-06-21 commit that removed this address as invented** — it was not invented, it was unverified, and the two are different dispositions. **Recheck before any future publish:** a mailbox is the one claim on this page that can stop being true without anyone touching the repo, so it has no code or registry to verify it and no gate can watch it. | VERIFIED (founder, Zoho admin console) | 2026-08-12 |

---

## Evidence-class audit, 2026-08-12

The banned-class proof in `.github/workflows/gates.yml` was repaired in #35 after it
was shown it could never fail: both controls searched for colour formats this
Tailwind does not emit. **No row in this file cited that proof as its evidence** —
searched by name and by phrase, zero matches — so no row is invalidated by it
directly.

Its failure class is not confined to it. A **denylist grep** — "these known-bad
strings are absent" — is only as complete as its author's list, and four defects in
this repo have now come from that shape: the font ban a stack could slip past, the
radius check that permitted everything between 2px and 9999px by accident, the
palette proof that could not fail, and a placeholder override that missed two of the
three rules it had to beat. Rows resting on a name denylist are weaker than they read.

| Row | Evidence shape | Disposition |
| --- | --- | --- |
| 19 | denylist grep over `.next` for analytics SDKs | **Superseded by row 62**, which re-verified against the current build by direct observation — zero external `src`/`href`, zero `<form>` in the rendered HTML of all five pages — and recorded the one false positive its own grep produced. No action. |
| 28 | `grep -rniE "openai\|anthropic\|litellm\|langchain\|transformers\|torch\|llm"` over the installed package | **Downgraded to UNVERIFIED (evidence class).** A vendor name nobody listed would pass. The claim is probably true and the published README asserts it independently, but the recorded evidence cannot carry it. To restore: enumerate the package's imports and allowlist them, or assert the absence of any network call in the scan path. |
| 54 | the same no-LLM grep, plus a live `list-checks` run | **Split.** The nine-check count is a live command and stands. The no-LLM half inherits row 28's downgrade. |
| 38, 42 | single `httpx.post` call site; command surface of exactly `scan`, `scan-config`, `list-checks`, `version` | **No action.** These are enumerations of what exists, not assertions that a list of bad names is absent. Different shape, sound evidence. |
| 32, 34 | historical records of a 0.1.0 defect | **No action.** Not live page claims. |

| 75 | Contact: "Pointing it at your own config takes **about ten seconds**" | contact page, before-you-write panel — **NOW CUT** | **REMOVED 2026-08-16 by `54b4efa`.** Recorded because it reveals a gate failure, not only a copy change: this sentence shipped with **no row in this file at all**, which the hard rule forbids. It was found by auditing the site for timing claims after a real run measured 6.33 s, and it survived earlier passes because rows 41 and 66 named the *hero* as the only place "ten seconds" appeared — so a grep of CLAIMS would not have found it, and a grep of the pages was never run. **The lesson is the lesson of rows 41 and 66, one level up:** an unfalsifiable number is hard to notice precisely because nothing ever contradicts it. Two numbers for one command (6.33 measured, 9.14 measured, "about ten" written) is what finally made it visible. Row 63 covers the surviving half of the sentence and is unaffected. | **REMOVED 2026-08-16 (`54b4efa`)** — never VERIFIED, because it never had a row | 2026-08-16 |
| 76 | Section 5 terminal block: the whole transcript, every line | home, section 5, the transcript pane | **Verbatim from a recorded run of the published artifact, character for character.** `evidence/2026-08-16-mcpscan-real-run/``run.cast` is one asciinema recording; `run.txt` is that cast replayed through a pyte terminal emulator at 120x34 and is the source the component's `LINES` array was transcribed from. The artifact is `orisan-mcpscan` 0.1.1 installed from PyPI by `uvx`, never the working tree, and the chain from published bytes to installed bytes is closed by `uv pip install --require-hashes` in `require-hashes.txt` — with the negative control, a one-digit-flipped digest, refused. The target is a byte-for-byte copy of this machine's real `~/.mcp.json`: one live `puppeteer` server, really spawned and really enumerated. The grade `F`, the two `CRITICAL (was HIGH)` rows on `puppeteer_evaluate` and the one `HIGH` on `puppeteer_navigate` are that server's actual findings. **Colour is ours, text is theirs:** the site's meaning-bound palette is applied to severity-bearing lines, and not one character of the output was changed, reflowed or tidied. | VERIFIED (agent, recorded run of the published artifact) | 2026-08-16 |
| 77 | Section 5 status line: "real 6.33s" | home, section 5, the transcript pane chrome | **A record of one machine, not a prediction about the reader's**, and the transcript prints the same figure so the two cannot drift. `/usr/bin/time -p` wrapped the very invocation asciinema captured, which is why `real 6.33` appears as a line *inside* the recording rather than as a number computed beside it. Conditions stated rather than implied: Apple M1 Pro, macOS 26.5.1, 2026-08-16, **cold uv cache** (`UV_CACHE_DIR` pointed at a fresh empty directory) and **warm npm cache**. **The npm caveat is load-bearing and is recorded in PROVENANCE.md:** `@modelcontextprotocol/server-puppeteer` is fetched by `npx -y` and was already cached, so a first-timer also pays a download this number excludes. 6.33s is a **floor**, not a typical figure, and must never be written as what it takes on a fresh machine. Supersedes the "about ten seconds" family of claims (rows 41, 66, 75), which were cut precisely because they could absorb any measurement. | VERIFIED (agent, timing captured inside the recording) | 2026-08-16 |
| 78 | Section 5 readout: grade `F` - "1 server scanned" - "3 findings — 2 CRITICAL, 1 HIGH" - "exit 1" | home, section 5, beneath the transcript | **Four readings, each a count of rows in the transcript directly above it, checkable by anyone who reads the pane.** `Servers: 1 total, 1 scanned, 0 failed, 0 skipped` and `Worst grade: F` are printed lines; the severity column contains exactly three finding rows, two `CRITICAL (was HIGH)` and one `HIGH`; `exit: 1` is the recorded exit status. The findings count has a second independent source: `mcpscan scan-config --output md` on the same config prints `Findings total: 3`. **These four were cut once**, because the reference's versions (`2 servers scanned / 2 findings at HIGH`) were invented and a page forbidding unmarked fabricated product output could not ship four of them. They return only because a real run now supplies all four. | VERIFIED (agent, transcript + second source) | 2026-08-16 |
| 79 | Section 5: "The process prints nothing for 3.4 of those seconds and then writes the whole report at once" | home, section 5, beneath the readout | Derived from the cast's own event timings, not from reading the source. `run.cast` replayed event by event: the last uv output lands at t=3.742s, then **nothing is emitted until t=7.178s** — a 3.436s gap — at which point the entire report arrives in **two adjacent writes 0.000s apart** (561 and 907 bytes), with every one of its 30 lines first appearing inside a 63ms window. The `/usr/bin/time` lines follow at 7.240s. **This row is also why section 5 has no animation:** a line-by-line replay would need per-line intervals and every real interval is 0ms, so any cadence would be a number someone chose. The component that preceded this one did exactly that, and its own "real runtime · 7.0s" was the sum of the invented waits. | VERIFIED (agent, cast event timings) | 2026-08-16 |

| 80 | Home section 5: "A server can tell the scanner what it is for. That can never count in its favour. Nothing a server says about itself makes its own grade better, and a config you copied from that server's own documentation is not you vouching for it either." | home, section 5, under the readout | **Verified against the published 0.1.1 wheel, not against the page this was folded in from.** `mcpscan/adjudicate.py` states the invariant in its own words: *"Any purpose source may ESCALATE a severity. Only an operator-supplied purpose may DOWNGRADE one"*, with the reasoning *"Escalation needs no trust, because the worst a hostile source achieves by escalating is making its own findings look worse. A downgrade is a claim that a dangerous capability is fine, so it may only come from someone outside the system under test."* `OPERATOR_PURPOSE_SOURCES = {PurposeSource.FLAG, PurposeSource.INVOCATION}` — a purpose read from a config file or from the server's own metadata is in neither set, so it cannot lower a severity. **"Never count in its favour" is the precise form and an earlier draft said "only ever count against it", which was wrong:** a server-declared purpose most often leaves severity UNCHANGED (`expected_unconfirmed`), and only escalates when nothing was declared (`undeclared`). Both verdicts are visible in the transcript directly above this paragraph, so a reader can check the sentence against the output without leaving the page. | VERIFIED (agent, package source of the published artifact) | 2026-08-17 |
| 81 | Home section 7: "Two things here are not proven… Both have to be attacked in the real thing, and that has not happened yet." | home, section 7, after the floor rule | **A disclosure of residual, and its accuracy is in what it declines to say — see row 83 for why it names nothing.** The two are real, distinct, and internally recorded as test obligations rather than closed proofs; the internal source states this twice, once as a standing limit and once per finding. The first is composition: whether a chain of individually-bounded actions reconstitutes something no single step is permitted. The second is deliberate false alarm: whether an attacker can drive the system to raise severity on false pretenses, blocking legitimate work and training the people watching it to stop listening. **The wording tracks the residual precisely** — the second finding has two harms and only the desensitization path is mitigate-only, which is the half the phrase "cry wolf, so the people watching it stop listening" names. "That has not happened yet" is true as of this date: no system-level adversarial testing has been run, and the external pen-test trigger sits at a later stage of the build programme. Supersedes the `/vision` sentence recorded in row 49, which was cut with that page in `e7fa5fa`. | VERIFIED (agent, internal source) | 2026-08-17 |
| 82 | Home section 7: "The map only works if we know which agent did what… The identity layer has to be real before anything is allowed to act on the map." | home, section 7, the dependency | **First-party design commitment**, and a statement of how the design constrains itself: a structure keyed on actor identity can be no more trustworthy than that identity. The internal model traces the compound failure — wrong identity means reach and findings are attributed to the wrong actor, and every decision consuming the map is wrong with it — and the build programme sequences the actor-identity plane before the map is trusted in enforcement. **Wording checked twice:** an earlier draft read "before the map is allowed to decide anything", which personifies the map; the map never decides, the gate decides using it, and the published form says *anything is allowed to act on the map*. Carries no identifier, severity or disposition, per row 83. Supersedes row 50, cut with `/vision` in `e7fa5fa`. Verification is a test obligation against the built system. | COMMITMENT — verification is a test obligation | 2026-08-17 |
| 83 | **Governance record for row 81: why that paragraph names nothing.** | — | **Shape without address, and it is a decision rather than vagueness.** Row 49 established that a residual may be published as its SHAPE and not its ADDRESS: no finding identifiers, no severities, no dispositions, no named areas. Row 50 recorded the enforcement of that rule — an earlier version of the page paraphrased a risk disposition and was cut, on the ruling that **a paraphrased disposition is still a published disposition**. The two things row 81 describes are identified findings in an internal document classified Restricted, carrying identifiers and severities. Naming either, or naming its severity, or saying where it lives, would breach rows 49 and 50. **Recorded because the absence is otherwise unreadable:** a sentence that claims two gaps and names neither looks like sloppiness to the next person who edits it, and the obvious "improvement" is to add the specifics. It is not an improvement. **The genuine cost is stated rather than hidden:** as published the sentence cannot be checked by a reader, so it trades falsifiability for classification, and it is the one claim on the site of which that is true. The compensating control is this row plus the comment in `FloorSection.tsx`, which name the constraint at both the claim and the code. | GOVERNANCE — decision recorded, not a page claim | 2026-08-17 |

| 84 | Home section 7: "Any part can raise suspicion. No part can grant permission." and "This is a commitment about our own design. Its verification is a test obligation against the built system." | home, section 7 | **First-party design commitment**, the same one rows 48 and 58 carry on the pages that stated it elsewhere. Not a claim about the world, so there is no external source to cite: this is a public statement of how Orisan is designed to behave, and we are the source. Verification is a test obligation against the built system, which is what the sentence says of itself. **This row exists because there was none.** Home's section 7 has carried this commitment since the home rebuild with no row in this file, and it surfaced only because a founder ruling deleted a clause from that same paragraph on 2026-08-17 and the row covering it was searched for and not found. Same shape as row 75: a claim that shipped unrowed, invisible to any grep of CLAIMS and findable only by reading the page and looking for its row. Rows 81 and 82 sit in the same section and were rowed when written; this one predates them. **Related open finding, deliberately not fixed here:** rows 38, 39, 42, 43, 46 and 47 quote text that is no longer anywhere on the site, having described the pre-rebuild home. They need marking REMOVED with the commit that removed each, which is research rather than a sweep, and is not this PR. | COMMITMENT — verification is a test obligation | 2026-08-17 |

Neither downgraded row supports text on any page: the no-LLM sentence appears on
`/product` sourced from row 54's live command half and from the published README, not
from the grep. Nothing on the site is currently unsupported.
